<ul>
  <% @projects.each do |project| %>
    <li><%= project.name %></li>
    <% if project.created_at.today? %>
      <li><%= project.created_at %></li>
    <% else %>
      <li><%= project.updated_at %></li>
    <% end %>
  <% end %>
</ul>